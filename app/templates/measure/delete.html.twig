{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.measure_delete'|trans({'%id%': measure.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.measure_delete'|trans({'%id%': measure.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('measure_delete', {id: measure.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('measure_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}